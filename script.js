//your JS code here. If required.
let student={
	name:"Neha",
	getKeys:(obj)=>{
		return Object.keys(obj);
	}
}
console.log(getKeys(student))
// function getKeys(obj){
// 	return Object.keys(obj);
// }

// console.log(getKeys(student))
